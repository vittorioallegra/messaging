import { User } from '../interfaces';

export const mockUser: User = {
  id: 'id',
  firstName: 'firstName',
  lastName: 'lastName',
  email: 'email',
  updatedAt: 'updatedAt',
  createdAt: 'createdAt',
  deletedAt: 'deletedAt',
};
